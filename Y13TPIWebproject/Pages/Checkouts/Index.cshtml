@page
@model Y13TPIWebproject.Pages.Checkouts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Checkout[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Checkout[0].TotalPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Checkout) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPrice)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CheckoutID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CheckoutID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CheckoutID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
